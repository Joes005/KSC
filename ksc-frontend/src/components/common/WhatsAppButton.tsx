import React from 'react';
import { useSiteData } from '../../services/SiteDataContext';

export default function WhatsAppButton() {
  // WhatsApp is cleanly integrated into the mobile bottom action bar and desktop side dock
  return null;
}
